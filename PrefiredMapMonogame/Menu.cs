using System;
using System.Collections.Generic;
using System.ComponentModel.Design;
using System.Linq;
using System.Text;
using System.Threading.Tasks;
using Microsoft.Xna.Framework;
using Microsoft.Xna.Framework.Graphics;
using Microsoft.Xna.Framework.Input;
using static System.Windows.Forms.VisualStyles.VisualStyleElement.Window;

namespace PrefiredMapMonogame
{
    public class Menu : Game1
    {
        //List<MenuButton> but = new List<MenuButton>();

        //public override void LoadContent()
        //{
        //    base.LoadContent();
        //}
        //MenuCommand startButton = new MenuCommand("Start", new Vector2(100, 100));
        //MenuCommand optionsButton = new MenuCommand("Options", new Vector2(100, 150));
        //MenuCommand exitButton = new("Exit", new Vector2(100, 200));
    }
}
